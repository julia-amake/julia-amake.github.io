import type { Meta } from '@storybook/react';
import { AppRouter } from './AppRouter';

const meta: Meta<typeof AppRouter> = {
  title: 'shared/AppRouter',
  component: AppRouter,
  args: {},
};

export default meta;

export const Default = {};
