import type { Meta } from '@storybook/react';
import { ProductCartList } from './ProductCartList';

const meta: Meta<typeof ProductCartList> = {
  title: 'shared/ProductCartList',
  component: ProductCartList,
  args: {},
};

export default meta;

export const Default = {};
