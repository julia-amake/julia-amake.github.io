import type { Meta } from '@storybook/react';
import { Form } from './Form';

const meta: Meta<typeof Form> = {
  title: 'shared/FormWrapper',
  component: Form,
  args: {},
};

export default meta;

export const Default = {};
